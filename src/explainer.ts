// The "explainer" MCP prompt body.
//
// This is intentionally a placeholder. The request was to mirror the content of
// the "explainer" skill (minus any attached webpage), but that skill is not
// available in this environment — so the prompt ships empty and ready to fill.
// Paste the explainer skill's text into EXPLAINER_PROMPT_TEXT below; the prompt
// definition in src/mcp.ts will pick it up automatically.
export const EXPLAINER_PROMPT_TEXT = ''
